- dotfiles:
    install:
      - vars:
          pkg_mgr: nix
        packages:
          - emacs
          - bats
      - vars:
          pkg_mgr: pip
          virtualenv: ~/.virtualenvs/global_virtualenv
        packages:
          - bumpversion
          - cookiecutter

- ansible-tasks